{
    "rules": {
        ".read": "auth != null",
        ".write": "auth != null",
        "users_status": {
            ".read": true,
            ".write": true
        },
        "stocks": {
            ".indexOn": [
                "status"
            ],
            "$stockId": {
                ".validate": "newData.hasChildren(['importDate', 'assetId', 'status'])",
                "qt_received": {
                    ".validate": "newData.isNumber()"
                },
                "qt_distributed": {
                    ".validate": "newData.isNumber()"
                },
                "qt_balance": {
                    ".validate": "newData.isNumber()"
                }
            }
        },
        "chatMessages": {
            "general": {
                ".indexOn": ["timestamp"]
            }
        }
    }
}